name: test
on:
  # Temporarily manual-only while automatic GitHub Actions runs are disabled.
  workflow_dispatch:
jobs:
  macos-arm64-native:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - run: make test
